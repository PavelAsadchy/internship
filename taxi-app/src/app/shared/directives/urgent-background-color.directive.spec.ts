import { UrgentBackgroundColorDirective } from './urgent-background-color.directive';

describe('UrgentBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new UrgentBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
